# Parent app origin(s) that embed this iframe, comma-separated, no trailing slash.
# Must include your main-app Vercel URL, e.g. https://your-main.vercel.app
VITE_ALLOWED_PARENT_ORIGINS=http://localhost:5174,http://127.0.0.1:5174

# Same as main-app (HMAC signing)
VITE_VAULT_SHARED_SECRET=
